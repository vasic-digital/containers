# Test Configuration: Docker Runtime
# This configuration tests containers running with Docker

CONTAINERS_REMOTE_ENABLED=false
# Force Docker runtime (overrides auto-detection)
# CONTAINERS_LOCAL_RUNTIME=docker
