# Test Configuration: Local Deployment
# This configuration tests containers running on the local machine

# Remote distribution disabled - all containers run locally
CONTAINERS_REMOTE_ENABLED=false

# Local runtime (auto-detected: podman or docker)
# CONTAINERS_LOCAL_RUNTIME=podman
