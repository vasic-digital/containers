# Test Configuration: Podman Runtime
# This configuration tests containers running with Podman

CONTAINERS_REMOTE_ENABLED=false
# Force Podman runtime (overrides auto-detection)
# CONTAINERS_LOCAL_RUNTIME=podman
