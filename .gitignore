# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Output of go coverage
*.out

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Go workspace
go.work
go.work.sum

# Vendor
vendor/

# Environment (contains secrets)
.env
.env.*
.service-registry/
/cmd/ctop/ctop
scripts/resource-policy/__pycache__/
*.pyc

# go-mutesting artifacts
report.json
go-mutesting-report.html
*.go.tmp

# Built CLI from cmd/crossbuild-matrix/ — Go binary, never committed (CONST-053)
/crossbuild-matrix

# CONST-053 round-299 sweep — explicit forbidden classes:
# Build artefacts
/bin/
/build/
/dist/
/out/
target/
*.a
*.o
*.class

# Cache / language-server state
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
.cache/
.gradle/

# Temp files
*.tmp
*~
*.orig
*.rej

# Sensitive (secret-leak intersection with CONST-042 / §11.4.10)
*.pem
*.key
*.crt
id_rsa*
id_ed25519*
.netrc
secrets/
api_keys.sh
.helix-manifest.yaml

# Logs / coverage / runtime captures
*.log
coverage.out
htmlcov/

# IDE / OS personal state already covered above; add JetBrains backups
.history/

# round-299 mutation-mode backup file (containers_describe_challenge.sh
# restores on EXIT trap but ignore here defends against trap interruption)
pkg/i18n/bundles/.active.*.yaml.mutate.bak
